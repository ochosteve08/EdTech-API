const {
  createModule,
  deleteModule,
  getModule,
  updateModule,
  getModules,
} = require('./module.services');

module.exports = {
  createModule,
  deleteModule,
  getModule,
  updateModule,
  getModules,
};
