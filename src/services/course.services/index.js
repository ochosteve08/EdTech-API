const {
  getCourses,
  createCourse,
  updateTheCourse,
  deleteCourseById,
} = require('./course.services');

module.exports = {
  getCourses,
  createCourse,
  updateTheCourse,
  deleteCourseById,
};
