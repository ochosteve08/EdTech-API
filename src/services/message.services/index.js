const {
  sendMessage,
  getMessage,
  fetchMessageHistory,
  deleteMessage,
} = require('./message.services');

module.exports = {
  sendMessage,
  getMessage,
  fetchMessageHistory,
  deleteMessage,
};
