const {
  createTeacher,
  updateTheTeacher,
  getTeacher,
  deleteTeacherById,
} = require('./teacher.services');

module.exports = {
  createTeacher,
  updateTheTeacher,
  getTeacher,
  deleteTeacherById,
};
