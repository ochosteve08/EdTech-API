const {
  sendMessage,
  getMessages,
  fetchMessageHistory,
  deleteMessage,
} = require('./messages.controller');

module.exports = {
  sendMessage,
  getMessages,
  fetchMessageHistory,
  deleteMessage,
};
