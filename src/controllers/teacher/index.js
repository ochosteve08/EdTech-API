const {
  createTeacher,
  updateTeacher,
  deleteTeacherById,
  getTeacher,
} = require('./teacher.controllers');

module.exports = {
  createTeacher,
  updateTeacher,
  deleteTeacherById,
  getTeacher,
};
