const {
  createModule,
  deleteModule,
  getModule,
  updateModule,
  getModules,
} = require('./module.controllers');

module.exports = {
  createModule,
  deleteModule,
  getModule,
  updateModule,
  getModules,
};
